
<div class = "container">
  <div class = "text-center">
    <% @articles.each do |article| %>
        <div class = "title">
        <h3><%= link_to article.title, article_path(article) %></h3>
        </div>
        <h4><%= article.content %></h3>
      <% end %>
  </div>
</div>
